$( document ).ready(function() {
    /* $('#slideshow img').each(function(){
        //var width = $(this).prop('naturalWidth');
        //console.log('this is ' + this.prop('naturalWidth'));
        console.log('this is ' + $(this).prop('naturalWidth'));
    }); */
});

// Perhaps the next move would be to listen for resize
// and check width and height and if either are above, resize
// image
/*
$( window ).resize(function() {
    // This works

    var activeImage = $('.sqs-active-slide img');
    alert('active image natural width is ' + activeImage.prop('naturalWidth'));

});
     */